import 'package:flutter/material.dart';

class AjudaIAScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ajuda da IA'),
      ),
      body: Center(
        child: Text('Conteúdo da Ajuda da IA'),
      ),
    );
  }
}
