import 'package:flutter/material.dart';

class TiragemAleatoriaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tiragem Aleatória'),
      ),
      body: Center(
        child: Text('Conteúdo da Tiragem Aleatória'),
      ),
    );
  }
}
